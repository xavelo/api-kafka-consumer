apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-kafka-consumer-deployment
  annotations:
    commitID: "d26a025b2f414a5df4f9a14841470303e3ed16d9"
spec:
  # replicas: 2 - to be managed by HPA
  selector:
    matchLabels:
      app: api-kafka-consumer
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: api-kafka-consumer
    spec:
      containers:
        - name: api-kafka-consumer
          image: 'xavelo/api-kafka-consumer:d26a025b2f414a5df4f9a14841470303e3ed16d9'
          resources:
            requests:
              cpu: "500m" # Set CPU request to 500m (0.5 CPU core)
              memory: "256Mi" # Optional, set memory request
            limits:
              cpu: "1000m" # Set CPU limit to 1000m (1 CPU core)
              memory: "512Mi" # Optional, set memory limit
          ports:
            - containerPort: 80
